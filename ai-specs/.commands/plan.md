---
description: Toma un spec existente y produce el plan de implementación (agente engineer)
argument-hint: [ruta al spec]
---

# /plan

Utilidad ligera. Toma un spec existente y produce el plan de implementación. Lo ejecuta el agente `engineer`, no el PM.

Spec: $ARGUMENTS

Salida: plan + tasks pequeñas, una a una.

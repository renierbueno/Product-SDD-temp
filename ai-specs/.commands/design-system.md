---
description: Invokes the establish-design-system skill to lock the high-fidelity visual identity
argument-hint: [brand context or direction to explore, if there's already a lead]
---

# /design-system

Invoke the `establish-design-system` skill (use the Skill tool with skill: "establish-design-system") with this context:

$ARGUMENTS

Follow the full process in `ai-specs/skills/establish-design-system/SKILL.md`: read `docs/doc_company_context.md` first, avoid the known clichés of AI-generated design, close on a locked decision (or explicitly mark ON HOLD, never ambiguous), and save the result to `docs/doc_design_system.md`. Remember: this is only for a high-fidelity version — a low-fidelity prototype doesn't need this command first.

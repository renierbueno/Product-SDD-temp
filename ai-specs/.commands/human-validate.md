---
description: Invokes the human-validation skill to execute the already-chosen method
argument-hint: [chosen method and who it's validated with]
---

# /human-validate

Invoke the `human-validation` skill (use the Skill tool with skill: "human-validation") with this method and context:

$ARGUMENTS

Follow the full process in `ai-specs/skills/human-validation/SKILL.md`: ask for commitment instead of opinion, respect that category's minimum sample size, use the script for the model's relevant side, and deliver that skill's expected output.
